# 生化培养箱控制系统 — 详细设计说明书 (修订版 V2.0)

> **文档版本**：V2.0  
> **修订日期**：2026年7月  
> **适用范围**：课程设计 / 项目报告参考资料  
> **MCU平台**：STM32F103C8T6 (ARM Cortex-M3, 72MHz)  
> **开发环境**：STM32CubeMX 6.15 + Keil MDK-ARM V5.32  
> **HAL库**：STM32Cube FW_F1 V1.8.7

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统总体设计](#2-系统总体设计)
3. [硬件详细设计](#3-硬件详细设计)
4. [软件详细设计](#4-软件详细设计)
5. [PID控制算法详解](#5-pid控制算法详解)
6. [MATLAB仿真与参数整定](#6-matlab仿真与参数整定)
7. [上位机通信协议](#7-上位机通信协议)
8. [调试与测试](#8-调试与测试)
9. [材料清单 BOM](#9-材料清单-bom)
10. [参考文献](#10-参考文献)

---

## 1. 系统概述

### 1.1 项目背景

生化培养箱是医疗卫生、食品检测、农林科研、院校实验室中不可或缺的设备，用于培养细菌、霉菌、酵母菌等微生物，完成菌落计数、致病菌检测、水质 BOD 测定、种子催芽、菌种保存等实验。其核心功能是提供精准稳定的恒温环境，温控范围通常为 5℃～50℃。

生化培养箱是一个典型的 **大惯性、大滞后** 被控对象：
- **大惯性**：箱体内空气的热容量大，温度变化缓慢，从加热器开启到温度上升需要数分钟。
- **大滞后**：加热器/制冷器与温度传感器之间存在物理距离，温度变化需要时间传递，纯滞后时间可达 30 秒以上。

因此，传统的开关式（bang-bang）控制无法满足精度要求，必须采用 **PID 控制** 甚至更高级的控制策略。

### 1.2 设计目标

设计并实现一个基于 STM32F103C8T6 微控制器的生化培养箱温度控制系统，满足以下要求：

| 指标 | 要求 |
|------|------|
| 控温范围 | 5℃ ~ 50℃ |
| 控制精度 | ±1℃ |
| 超调量 | < 20% |
| 控制方式 | 温度检测 → PID 计算 → PWM 驱动 → 加热/制冷 |
| 人机交互 | 4×3 矩阵键盘输入 + OLED 实时显示 |
| 上位机通信 | USART1 串口通信，实时上报 + 远程控制 |
| 报警功能 | 温度超限蜂鸣器报警，偏差 > 2℃ 触发 |

### 1.3 系统功能

1. **温度检测**：DS18B20 数字温度传感器实时采集箱内温度，12 位分辨率，精度 ±0.5℃
2. **温度控制**：位置式 PID 算法（积分分离 + 微分对测量值 + 抗积分饱和）
3. **执行机构**：BTS7960 H 桥驱动模块统一控制加热片和半导体制冷片（TEC）
4. **人机交互**：4×3 矩阵键盘设定目标温度，128×64 OLED 屏幕显示运行信息
5. **上位机通信**：USART1 串口通信，支持实时数据上报和远程命令控制
6. **报警保护**：温度偏差超过 2℃ 时蜂鸣器报警

---

## 2. 系统总体设计

### 2.1 系统组成框图

```
┌─────────────────────────────────────────────────────────────┐
│                        220V 交流电源                          │
│                             │                                │
│                     220V→12V 开关电源                         │
│                        12V / 2A                              │
│                     ┌──────┴──────┐                          │
│                     │  12V→5V     │  12V→3.3V                │
│                     │  (7805)     │  (AMS1117)               │
│                     │    5V       │    3.3V                  │
│                     └──────┬──────────────┐                  │
│                            │              │                  │
│                    ┌───────┴──────────────┘                  │
│                    │       STM32F103C8T6                     │
│                    │    ┌────────────────┐                   │
│                    │    │  ARM Cortex-M3  │                   │
│                    │    │  72MHz, 64KB FLASH                 │
│                    │    │  20KB SRAM       │                   │
│                    │    └──┬──┬──┬──┬──┬──┘                   │
│                    │       │  │  │  │  │                      │
│                    │       │  │  │  │  └──────┐               │
│                    │       ▼  ▼  ▼  ▼         ▼               │
│                    │   OLED DS18B20 按键 蜂鸣器 UART→ PC    │
│                    │   (I2C) (1-Wire)      (GPIO) (USART1)   │
│                    │       │                                   │
│                    │       └──────────┐                       │
│                    │                  ▼                       │
│                    │        ┌─────────────────┐               │
│                    │        │   BTS7960 H桥   │               │
│                    │        │  (大电流驱动)   │               │
│                    │        └───┬─────────┬───┘               │
│                    │            │         │                   │
│                    │            ▼         ▼                   │
│                    │      ┌────┴─┐  ┌────┴──────┐            │
│                    │      │加热片│  │ TEC制冷片  │            │
│                    │      │12V10W│  │12V60W     │            │
│                    │      └──────┘  └────┬──────┘            │
│                    │                     │                    │
│                    │               ┌─────┴─────┐              │
│                    │               │ 热端散热片 │              │
│                    │               │ + 散热风扇 │              │
│                    │               └─────┬─────┘              │
│                    │                     │                    │
│                    │               热风排出箱外               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 控制策略总览

系统采用 **位置式 PID 控制**，控制周期 200ms，PID 输出范围 `[-100, +100]`，直接映射为 BTS7960 的 PWM 占空比。控制区段划分如下：

```
PID输出 u     PWM 占空比             运行模式
─────────     ────────────           ────────
u > +5%       u% (最小5%)            比例加热
-5% ≤ u ≤ +5%  0% (停止)            死区
u < -5%       |u|% (最小5%)          比例制冷
```

**关键算法特性：**
- **积分分离**：误差 `|e| > 3℃` 时禁用积分项，防止大幅超调
- **微分对测量值**：微分项对温度测量值求导，避免设定值突变时的微分冲击
- **独立积分限幅**：积分累积值独立限幅，确保 `Ki * integral ≤ 80% * output_limit`
- **抗积分饱和**：输出限幅时条件积分法，防止积分深度饱和

### 2.3 软件架构层次

```
┌──────────────────────────────────────────────────────────────┐
│                       应用层 (App Layer)                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ main.c   │ │ key.c    │ │ oled.c   │ │usart_com │ │ rtc.c    │
│  │ 主循环调度│ │ 键盘扫描  │ │ OLED显示  │ │UART通信  │ │ RTC驱动  │
│  │ 状态机    │ │ 按键处理  │ │ 字库渲染  │ │ 命令解析  │ │ 时间读取  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘
├──────────────────────────────────────────────────────────────┤
│                      控制层 (Control Layer)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │
│  │ pid.c    │ │ control.c│ │ ds18b20.c│                     │
│  │ PID算法   │ │ BTS7960  │ │ 1-Wire   │                     │
│  │ 积分分离  │ │ PWM控制  │ │ 时序驱动  │                     │
│  │ 抗饱和    │ │ 区段映射  │ │ 温度读取  │                     │
│  └──────────┘ └──────────┘ └──────────┘                     │
├──────────────────────────────────────────────────────────────┤
│                      HAL 抽象层 (HAL Layer)                    │
│  STM32F1xx_HAL_Driver (GPIO / I2C / TIM / UART / RTC / RCC)        │
├──────────────────────────────────────────────────────────────┤
│                      硬件层 (Hardware Layer)                   │
│  STM32F103C8T6 + DS18B20 + SSD1306 + BTS7960 + Matrix Keypad + RTC│
└──────────────────────────────────────────────────────────────┘
```

---

## 3. 硬件详细设计

### 3.1 核心控制器 — STM32F103C8T6

**选型理由：**
- ARM Cortex-M3 内核，72MHz 主频，满足 PID 实时计算需求
- 64KB Flash / 20KB SRAM，代码空间充裕
- 内置 3 个 USART，满足上位机通信需求
- 丰富的定时器资源，可产生多路独立 PWM
- 价格低廉，开发资料丰富，适合课程设计

**系统时钟树：**

```
HSE 8MHz ─→ PLL(x9) ─→ SYSCLK 72MHz
                            ├── AHB  ─→ 72MHz (HCLK)
                            ├── APB1 ─→ 36MHz (PCLK1, max)
                            └── APB2 ─→ 72MHz (PCLK2, USART1 时钟源)
                            LSE 32.768kHz ─→ RTC 时钟
```

### 3.2 引脚分配表

| STM32引脚 | 连接模块 | 功能 | 配置 | 备注 |
|:----------|:---------|:-----|:-----|:-----|
| **PA0** | DS18B20 DQ | 1-Wire 数据线 | 开漏输出/浮空输入 | 需 4.7kΩ 上拉到 3.3V |
| **PB6** | OLED SCL | I2C1 时钟线 | 复用开漏输出 | 外接 4.7kΩ 上拉 |
| **PB7** | OLED SDA | I2C1 数据线 | 复用开漏输出 | 外接 4.7kΩ 上拉 |
| **PA6** | BTS7960 IN1 | PWM 输出 (加热) | 复用推挽输出 | TIM3_CH1, 10kHz |
| **PA7** | BTS7960 IN2 | PWM 输出 (制冷) | 复用推挽输出 | TIM3_CH2, 10kHz |
| **PB0** | 矩阵键盘行 0 | 行扫描输出 | 推挽输出 | 初始低电平 |
| **PB1** | 矩阵键盘行 1 | 行扫描输出 | 推挽输出 | 初始低电平 |
| **PB10** | 矩阵键盘行 2 | 行扫描输出 | 推挽输出 | 初始低电平 |
| **PB11** | 矩阵键盘行 3 | 行扫描输出 | 推挽输出 | 初始低电平 |
| **PB12** | 矩阵键盘列 0 | 列检测输入 | 上拉输入 | 检测高电平 |
| **PB13** | 矩阵键盘列 1 | 列检测输入 | 上拉输入 | 检测高电平 |
| **PB14** | 矩阵键盘列 2 | 列检测输入 | 上拉输入 | 检测高电平 |
| **PA8** | 有源蜂鸣器 | 报警输出 | 推挽输出 | 低电平有效，经 NPN 三极管驱动 |
| **PA9** | USB-to-TTL TXD | USART1 TX | 复用推挽输出 | 连接 USB 转 TTL 模块的 RXD |
| **PA10** | USB-to-TTL RXD | USART1 RX | 上拉输入 | 连接 USB 转 TTL 模块的 TXD |
| **PA13** | SWDIO | 调试接口 | — | 仅调试使用 |
| **PA14** | SWCLK | 调试接口 | — | 仅调试使用 |

### 3.3 电源电路

系统采用三级电源架构：

```
220V AC ─→ 12V/2A 开关电源 ─┬─→ BTS7960 (模块供电)
                             ├─→ 加热片 (12V/10W)
                             ├─→ TEC1-12706 (12V/60W)
                             ├─→ 8015 散热风扇 (12V/2W)
                             ├─→ 7805 ─→ 5V ─┬─→ OLED (SSD1306)
                             │               ├─→ DS18B20 (VDD)
                             │               └─→ 蜂鸣器 (经三极管)
                             └─→ AMS1117-3.3 ─→ 3.3V ─→ STM32F103C8T6
```

**设计要点：**
- 12V 主电源：为加热片 (10W)、TEC 制冷片 (60W)、风扇 (2W) 供电，总功耗约 72W，选用 12V/2A 开关电源余量充足
- 5V 逻辑电源：使用 7805 从 12V 降压，为 OLED、DS18B20、蜂鸣器供电
- 3.3V 核心电源：使用 AMS1117-3.3 低压差稳压器，为 STM32 核心板供电
- 各电源域共地，BTS7960 的 GND 与 STM32 GND 相连

### 3.4 BTS7960 H桥驱动电路

**模块简介：**
BTS7960 是 Infineon 集成的半桥驱动芯片（相当于一个高侧 PMOS + 低侧 NMOS 的组合），通常两片组成一个 H 桥。市售的 BTS7960 驱动模块已将两片芯片集成在一块 PCB 上，构成完整 H 桥。

**与负载的接线方案：**

BTS7960 H 桥模块有两个输出端子 M+ 和 M-，两个负载分别对地连接：

```
BTS7960 模块                  负载侧
─────────────                 ────────
B+ (12V) ───────────── 12V 电源正极
B- (GND) ───────────── 12V 电源负极 (GND)

                            ┌────────┐
M+ ──────────────────────── │ 加热片 │ ──→ B- (GND)
                            └────────┘
                            ┌────────┐
M- ──────────────────────── │ TEC    │ ──→ B- (GND)
                            └────────┘

IN1 (PA6) = 加热 PWM 输入
IN2 (PA7) = 制冷 PWM 输入
EN       = 拉高到 5V 使能
```

**工作原理：** 任一时刻只有 M+ 或 M- 中的一个对 GND 有 PWM 电压，另一个被 H 桥内部拉到 GND。两个负载永远不会同时得电。

**工作模式：**

| 模式 | IN1 (PA6) | IN2 (PA7) | 输出 | 效果 |
|------|-----------|-----------|------|------|
| 加热 | PWM (duty) | LOW | M+=PWM, M-=GND | 加热片得电，TEC 两端等电位 |
| 制冷 | LOW | PWM (duty) | M+=GND, M-=PWM | TEC 得电，加热片两端等电位 |
| 停止 | LOW | LOW | M+=GND, M-=GND | 均断电 |

**软件互锁**：代码确保 IN1 和 IN2 不会同时输出 PWM（即任一时刻只加热或只制冷）。

**硬件保护**：BTS7960 芯片内置过流保护、过温保护、欠压关断和逻辑互锁，防止直通短路。

### 3.5 温度传感器 — DS18B20

**关键参数：**

| 参数 | 值 |
|------|-----|
| 测温范围 | -55℃ ~ +125℃ |
| 精度 | ±0.5℃ (-10℃ ~ +85℃) |
| 分辨率 | 9~12 位可编程（默认 12 位） |
| 转换时间 | 12 位模式下最大 750ms |
| 通信接口 | 1-Wire 单总线 |
| 供电方式 | 外部供电（VDD 接 3.3V/5V） |

**1-Wire 时序要求：**

```
复位脉冲: 主机拉低 480μs → 释放 → 设备拉低 60~240μs 应答
写 "1":   主机拉低 1~15μs → 释放 → 采样窗口 60μs
写 "0":   主机拉低 60μs → 释放 → 恢复时间 2μs
读时序:   主机拉低 1~15μs → 释放 → 15μs 内采样
```

**非阻塞读取策略：**
为避免 750ms 阻塞影响控制周期，代码采用两段式异步读取：
1. `DS18B20_StartConversion()` — 发送转换命令，立即返回
2. 等待 4 个控制周期（800ms > 750ms）
3. `DS18B20_ReadResult()` — 读取温度结果

### 3.6 OLED 显示模块 — SSD1306

| 参数 | 值 |
|------|-----|
| 型号 | 0.96 寸 OLED，SSD1306 驱动 |
| 分辨率 | 128 × 64 像素 |
| 接口 | I2C (SCL=PB6, SDA=PB7) |
| 设备地址 | 0x3C (7 位) |
| 工作电压 | 3.3V ~ 5V |
| 显存组织 | 128 列 × 8 页（每页 8 像素） |

**显示刷新**：采用双缓冲机制。代码操作显存缓冲区，然后一次性将全屏数据（1024 字节）通过 I2C 写入 SSD1306。

**字库支持：**
- Font6x8：6×8 像素，95 个 ASCII 字符（0x20~0x7E）
- Font8x16：8×16 像素，95 个 ASCII 字符（0x20~0x7E），用于标题显示

### 3.7 4×3 矩阵键盘

**按键布局：**

```
┌───┬───┬───┐
│ 1 │ 2 │ 3 │   ← ROW0 (PB0)
├───┼───┼───┤
│ 4 │ 5 │ 6 │   ← ROW1 (PB1)
├───┼───┼───┤
│ 7 │ 8 │ 9 │   ← ROW2 (PB10)
├───┼───┼───┤
│ * │ 0 │ # │   ← ROW3 (PB11)
└───┴───┴───┘
  ↑   ↑   ↑
COL0  COL1  COL2
PB12  PB13  PB14
```

**扫描原理（逐行扫描法）：**
1. 所有行输出低电平
2. 逐行输出高电平
3. 读取列引脚，若某列为高则检测到按键
4. 软件消抖（延时 10ms + 等待释放）

**按键功能：**

| 按键 | 功能 |
|------|------|
| `*` | 进入温度设置模式 / 退格 |
| `#` | 确认设定并开始运行 / 运行-空闲切换 |
| `0`~`9` | 数字输入 |
| 2 位数字 (如 37) | 整数温度 (37℃) |
| 3 位数字 (如 375) | 含一位小数 (37.5℃) |

### 3.8 USART1 串口通信电路

**硬件连接：**

STM32F103C8T6 内置 3 路 USART，本系统使用 USART1 与上位机通信。

| STM32 引脚 | USB-to-TTL 模块 | 功能 |
|-----------|-----------------|------|
| PA9 (USART1_TX) | RXD | STM32 发送 → 上位机接收 |
| PA10 (USART1_RX) | TXD | 上位机发送 → STM32 接收 |
| GND | GND | 共地 |

**USB-to-TTL 模块**：使用 CH340、CP2102 或 FT232 模块将 USART1 的 TTL 电平转换为 USB，连接 PC。需安装对应驱动。

**通信参数：**
- 波特率：115200
- 数据位：8
- 停止位：1
- 校验：无
- 流控：无

**接收方式**：轮询接收。主循环每次迭代调用 `UART_ProcessRx()` 检查 UART_FLAG_RXNE 标志，读取已接收字节并按 `\r\n` 换行符分割为命令帧。

### 3.9 散热系统设计

半导体制冷片（TEC1-12706）基于帕尔帖效应工作：
- **冷面**（箱体内）吸收热量，温度降低
- **热面**（箱体外）释放热量，温度升高
- 热面若得不到有效散热，温度会急剧上升，导致 TEC 损坏甚至烧毁

**物理布局：**
```
箱体内部                 箱体壁             箱体外部
─────────               ──────             ─────────
TEC冷面 ── 导热硅脂 ── [隔热层] ── 导热硅脂 ── TEC热面
   │                                             │
   ▼                                             ▼
冷端铝块                                   热端鳍片散热片
(均匀扩散冷气)                                + 8015 风扇
                                            (持续吹风散热)
```

**散热风扇参数：**
- 型号：8015 直流无刷风扇（80×80×15mm）
- 供电：12V 直接连接电源母线
- 控制：系统通电即持续运转，不参与 PID 控制

---

## 4. 软件详细设计

### 4.1 开发环境与工具链

| 工具 | 版本 | 用途 |
|------|------|------|
| STM32CubeMX | 6.15.0 | MCU 引脚配置、时钟树、外设初始化代码生成 |
| Keil MDK-ARM | V5.32 | 编译、链接、调试 |
| ARM Compiler | V5/V6 | C 代码编译 |
| ST-Link | — | 下载与调试 |
| HAL 库 | STM32Cube FW_F1 V1.8.7 | 硬件抽象层 |

### 4.2 工程文件结构

```
temperature_control/
├── Core/
│   ├── Inc/                           # 头文件
│   │   ├── main.h                     # 系统全局定义
│   │   ├── gpio.h                     # GPIO 初始化
│   │   ├── i2c.h                      # I2C1 初始化
│   │   ├── tim.h                      # TIM3 PWM 配置
│   │   ├── usb.h                      # USB PCD 配置 (保留, 未编译)
│   │   ├── stm32f1xx_it.h            # 中断服务
│   │   ├── stm32f1xx_hal_conf.h      # HAL 模块配置
│   │   ├── ds18b20.h                 # DS18B20 驱动
│   │   ├── oled.h                    # OLED 驱动
│   │   ├── key.h                     # 矩阵键盘驱动
│   │   ├── pid.h                     # PID 控制器
│   │   ├── control.h                 # BTS7960 控制
│   │   ├── usart_com.h              # USART1 串口通信
│   │   └── rtc.h                    # RTC 实时时钟
│   └── Src/                           # 源文件
│       ├── main.c                     # 主程序
│       ├── gpio.c                     # GPIO 配置
│       ├── i2c.c                      # I2C1 配置
│       ├── tim.c                      # TIM3 PWM 配置
│       ├── usb.c                      # USB PCD 配置 (保留, 未编译)
│       ├── stm32f1xx_it.c            # 中断服务
│       ├── stm32f1xx_hal_msp.c       # HAL MSP 初始化
│       ├── system_stm32f1xx.c        # 系统时钟
│       ├── ds18b20.c                 # DS18B20 驱动
│       ├── oled.c                    # OLED 驱动
│       ├── key.c                     # 矩阵键盘驱动
│       ├── pid.c                     # PID 控制器
│       ├── control.c                 # BTS7960 控制
│       ├── usart_com.c              # USART1 串口通信
│       └── rtc.c                    # RTC 实时时钟
├── Drivers/                           # ST 官方驱动库
│   ├── CMSIS/                         # ARM CMSIS 内核
│   └── STM32F1xx_HAL_Driver/          # HAL 外设驱动
├── MDK-ARM/                           # Keil 工程文件
│   ├── temperature_control.uvprojx    # 项目文件
│   ├── temperature_control.uvoptx     # 选项文件
│   └── startup_stm32f103xb.s          # 启动文件
└── temperature_control.ioc            # CubeMX 配置文件
```

### 4.3 主程序流程 (main.c)

#### 4.3.1 初始化流程

```
系统上电
  │
  ├─ HAL_Init()                    // HAL 库初始化
  ├─ SystemClock_Config()          // 时钟: HSE→PLL→72MHz
  │
  ├─ MX_GPIO_Init()                // GPIO 引脚配置
  ├─ MX_I2C1_Init()                // I2C1 400kHz
  ├─ MX_TIM3_Init()                // TIM3 PWM 10kHz
  ├─ MX_USART1_UART_Init()         // USART1 115200-8N1
  ├─ MX_RTC_Init()                 // RTC 1Hz (LSE 32.768kHz)
  │
  ├─ OLED_Init()                   // SSD1306 初始化
  ├─ Display_Splash()              // 显示启动画面 (2秒)
  │
  ├─ DS18B20_Init()                // 检测传感器存在
  ├─ Key_Init()                    // 键盘初始化
  │
  ├─ PID_Init(Kp=15, Ki=0.8, Kd=2.5, T=0.2s, lim=±100)
  ├─ Control_Init()                // 启动 PWM 输出
  ├─ UART_Init()                   // 启动 USART1 (115200-8N1)
  ├─ HAL_RTC_SetTime()             // RTC 初值 = 00:00:00
  │
  ├─ DS18B20_StartConversion()     // 首次温度转换
  ├─ HAL_Delay(750)                // 等待转换完成
  ├─ current_temp = DS18B20_ReadResult()
  │
  └─ 进入主循环
```

#### 4.3.2 主循环

```
while (1)
{
    now = HAL_GetTick();

    /* ① 按键扫描 (每轮主循环, 不依赖 200ms) */
    key = Key_Scan();
    if (key != KEY_NONE)
    {
        ProcessKey(key);
        Display_Update();          // 立刻刷新 OLED, 不等 200ms
    }

    if (now - last_tick >= 200ms)
    {
        /* ② 温度采集 (非阻塞) */
        if (ds18b20_conv_started)
            current_temp = DS18B20_ReadResult();
        DS18B20_StartConversion();
        ds18b20_conv_started = 1;

        /* ③ 报警检测 (始终执行) */
        Alarm_Check();

        /* ④ PID 控制 (仅运行态) */
        if (sys_state == SYSTEM_STATE_RUNNING)
        {
            pid_out = PID_Calculate(target, current);
            Control_ProcessOutput(pid_out);
            run_time_ms += 200;
            if (run_time_ms >= 1000) {
                run_time_sec++;
                run_time_ms -= 1000;
            }
        }

        /* ⑤ 定时刷新 OLED (200ms 周期) */
        Display_Update();
    }

    /* 每 1s 串口上报 */
    if (now - last_uart_tick >= 1000ms)
    {
        UART_SendData();
        last_uart_tick = now;
    }

    /* 串口接收 & 命令处理 */
    UART_ProcessRx();
    ProcessUartCmd();
}
```

#### 4.3.3 系统状态机

```
           ┌──────────┐
           │  上电     │
           └────┬─────┘
                │
           ┌────▼─────┐   按 * 键    ┌──────────┐
           │  IDLE    ├─────────────►│ SETTING  │
           │  空闲    │              │ 温度设定  │
           └──┬──┬────┘              └────┬─────┘
              │  │                        │
        按 #  │  │ 按 #                   │ 按 # (确认)
              │  │              ┌─────────┘
              ▼  │              ▼
         ┌───────┴──┐      ┌──────┴──────┐
         │ RUNNING  │◄──── │  温度值有效  │
         │  运行态   │      │  开始运行   │
         └──┬───┬───┘      └─────────────┘
            │   │
      温度超限 │  │ 温度恢复
            │   │
            ▼   │
         ┌──────┴───┐
         │  ALARM   │
         │  报警态   │
         └──────────┘

状态转换说明:
  IDLE → SETTING:   按 * 键
  SETTING → SETTING: 按数字键(输入)、按 *(退格)
  SETTING → RUNNING: 按 # 键(确认)
  RUNNING → IDLE:   按 # 键(停止)
  IDLE → RUNNING:   按 # 键(直接启动)
  RUNNING → ALARM:  温度偏差 > 2℃
  ALARM → RUNNING:  温度偏差回到正常范围
```

### 4.4 模块详解 — DS18B20 驱动 (ds18b20.c/h)

**文件职责：** 实现 1-Wire 总线时序和 DS18B20 温度数据读取。

**关键函数：**

| 函数 | 说明 |
|------|------|
| `DS18B20_Init()` | 初始化 DWT 定时器，复位总线并检测传感器存在 |
| `DS18B20_ResetPulse()` | 产生复位脉冲，返回 1=设备存在 / 0=无设备 |
| `DS18B20_WriteByte(byte)` | 按 1-Wire 时序写入一个字节 (LSB first) |
| `DS18B20_ReadByte()` | 按 1-Wire 时序读取一个字节 (LSB first) |
| `DS18B20_StartConversion()` | 向传感器发送 0x44 转换命令，立即返回 |
| `DS18B20_ReadResult()` | 读取暂存器第 0~1 字节，计算温度值 = raw × 0.0625 |
| `DS18B20_GetTemperature()` | 阻塞式：StartConversion → 750ms 等待 → ReadResult |

**微秒延迟实现：**
使用 DWT（Data Watchpoint and Trace）硬件周期计数器实现精确微秒延时，不依赖编译器优化等级。DWT 以 CPU 核心时钟（72MHz）计数，每个 tick = 13.89ns。

**温度数据格式：**
```
暂存器 Byte 0 (LSB)           Byte 1 (MSB)
┌─────────┬─────────┐    ┌─────────┬─────────┐
│  7  6  5  4  3  2  1  0 │  7  6  5  4  3  2  1  0 │
│ 2³ 2² 2¹ 2⁰ 2⁻¹2⁻²2⁻³2⁻⁴│ S  S  S  S  S 2⁶ 2⁵ 2⁴ │
└─────────┴─────────┘    └─────────┴─────────┘
温度 = (MSB<<8 | LSB) × 0.0625 ℃
S = 符号位 (1=负温度)
```

### 4.5 模块详解 — PID 控制器 (pid.c/h)

**算法类型：** 位置式 PID（微分对测量值）

**数学公式：**

```
u(k) = Kp × e(k) + Ki × Σ[e(j) × Ts] - Kd × [y(k) - y(k-1)] / Ts

其中:
  u(k)  = 第 k 次控制输出  [-100, +100]
  e(k)  = 设定值 - 测量值  (偏差)
  y(k)  = 第 k 次测量值   (温度)
  Ts    = 采样周期 = 0.2s
  Kp    = 比例增益 = 15.0
  Ki    = 积分增益 = 0.8
  Kd    = 微分增益 = 2.5
```

**算法改进：**

1. **积分分离 (Integral Separation)**
   ```
   if |e| > 3℃:  Ki = 0  (禁用积分，防止大幅超调)
   if |e| ≤ 3℃:  Ki = 0.8 (启用积分，消除稳态误差)
   ```

2. **微分对测量值 (Derivative on Measurement)**
   ```
   传统: d_term = Kd × [e(k) - e(k-1)] / Ts
   改进: d_term = -Kd × [y(k) - y(k-1)] / Ts
   ```
   **原因**：设定值变化时，传统微分会产生尖峰输出（微分冲击），对测量值求导则不受设定值变化影响。

3. **独立积分限幅**
   ```
   integral_limit = output_limit × 0.8 / Ki = 100 × 0.8 / 0.8 = 100
   integral 被限制在 [-100, +100] 范围内
   ```
   **原因**：防止积分项过度累积，确保 P 项和 D 项仍有足够的输出范围。

4. **抗积分饱和 (Anti-Windup)**
   ```
   if u(k) > +100:
       u(k) = +100
       if e > 0: integral -= e × Ts    (撤消积分累积)
   if u(k) < -100:
       u(k) = -100
       if e < 0: integral -= e × Ts
   ```
   **原理**：条件积分法 — 当输出饱和时，如果误差方向与饱和方向一致，则回退积分累积，避免积分深度饱和。

### 4.6 模块详解 — BTS7960 控制 (control.c/h)

**PWM 配置：**
- TIM3 时钟 = APB1 时钟 × 2 = 36MHz × 2 = 72MHz (因 TIM 重映射)
- ARR (Period) = 7199 → PWM 频率 = 72MHz / (7199+1) = 10kHz
- 计数范围: 0 ~ 7199
- 占空比计算公式: `CCR = duty% × 7200 / 100`

**控制区段映射：**

```
PID 输出 u     →  占空比          →  IN1    IN2    加热片  TEC
─────────────────────────────────────────────────────────────
u > +5%        →  u% (最小5%)     →  PWM    LOW     工作   停止
-5% ≤ u ≤ +5%  →  0%              →  LOW    LOW    停止   停止
u < -5%        →  |u|% (最小5%)   →  LOW    PWM    停止   工作
```

**最小占空比 5%**：防止极低占空比时 BTS7960 进入不连续导通模式，产生可闻噪声。

### 4.7 模块详解 — OLED 显示 (oled.c/h)

**显示布局：**

```
┌──────────────────────────────────┐  ←─ y=0
│        Bio-Incubator             │  ←─ Font8x16 标题
├──────────────────────────────────┤
│  Now:  37.0 C                    │  ←─ y=16, Font6x8
│  Set:  37.0 C                    │  ←─ y=24, Font6x8
│  Status: RUN / IDLE / ALARM      │  ←─ y=32, Font6x8
│  PID: +15.3%                     │  ←─ y=40, Font6x8
│  14:32:05 Run:1234 s            │  ←─ y=48, Font6x8 (RTC 时间 + 运行秒数)
└──────────────────────────────────┘
```

**温度设定模式：**

```
┌──────────────────────────────────┐
│        Bio-Incubator             │
├──────────────────────────────────┤
│  Now:  37.0 C                    │
│  Set:  37.0 C                    │
│  Status: SETTING                 │
│  Set: 37                         │  ←─ 实时显示输入数字
│  Input Temp...                   │
└──────────────────────────────────┘
```

**刷新策略：**
- 定时刷新：每 200ms 控制周期调 `Display_Update()` 一次，重绘所有内容
- 按键触发：检测到按键后立即 `Display_Update()`，不等 200ms 周期，实现即时反馈
- 双缓冲：写入显存缓冲区 → 一次性 `OELD_Display()` 刷到硬件
- 清屏优化：`OLED_Clear()` 仅清零缓冲区，不触发硬件刷新（由 `Display_Update` 统一刷新），避免二次 I2C 传输

### 4.8 模块详解 — 矩阵键盘 (key.c/h)

**扫描算法（逐行法，非阻塞）：**

```
for each 行 r:
    所有行输出 LOW
    第 r 行输出 HIGH
    ~2µs 等待信号稳定
    for each 列 c:
        if 列 c == HIGH:
            found = key_map[r][c]
            break
    if found != KEY_NONE: break

if found != KEY_NONE && found != last_key:
    last_key = found
    return found
return KEY_NONE
```

**非阻塞设计要点：**

| 措施 | 说明 |
|------|------|
| 无 `HAL_Delay` / `while(等待释放)` | 按键按下时不阻塞主循环，温度采集 / PID / 串口继续运行 |
| 50ms 时间戳去抖 | 触发一次后屏蔽 50ms（基于 `HAL_GetTick`），替代硬件消抖 |
| 同键防重 | `last_key` 静态变量，连续扫描到相同键值自动忽略，松开后才重新触发 |
| ~2µs 行稳定延迟 | GPIO 设置行后微短延时再读列，确保信号已到达列引脚 |

**按键编码映射表：**

```c
key_map[4][3] = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9},
    {0x0A, 0, 0x0B}    // 0x0A = *, 0x0B = #
};
```

### 4.9 模块详解 — USART1 通信 (usart_com.c/h)

**通信参数：**

| 参数 | 值 |
|------|-----|
| 外设 | USART1 |
| 引脚 | TX=PA9, RX=PA10 |
| 波特率 | 115200 bps |
| 数据位/停止位/校验 | 8N1 |
| 接收方式 | 轮询 UART_FLAG_RXNE |
| 命令分隔符 | `\r\n` (CR+LF) |

**数据上报格式：**
```
T:25.5,SET:37.0,PID:45.0,HT:60%,CL:0%,RUN:120s\r\n
↑当前温  ↑目标温  ↑PID输出  ↑加热占空比 ↑制冷占空比 ↑运行秒数
```

**上位机命令格式：**

| 命令 | 示例 | 响应 | 说明 |
|------|------|------|------|
| `SET:x.x` | `SET:37.5\r\n` | `OK:37.5\r\n` 或 `ERR:RANGE\r\n` | 设定目标温度 |
| `STOP` | `STOP\r\n` | `OK:STOPPED\r\n` | 停止控温 |
| `STATUS` | `STATUS\r\n` | 同上报格式 | 查询当前状态 |

**数据流：**
```
上位机 → USB-to-TTL → PA10 (USART1_RX)
                         → UART_ProcessRx() 轮询接收字节
                         → 检测 \n 换行 → uart_rx_flag = 1
                         → 主循环 ProcessUartCmd() 解析处理

主循环 → UART_Printf() → HAL_UART_Transmit()
                         → PA9 (USART1_TX) → USB-to-TTL → 上位机接收
```

### 4.10 报警逻辑

**触发条件：** `|当前温度 - 目标温度| > 2℃`

**行为：**
1. 系统状态切换为 `SYSTEM_STATE_ALARM`
2. PA8 输出低电平，蜂鸣器持续鸣响
3. OLED 显示 "Status: ALARM"

**恢复条件：** 温度恢复到 `|偏差| ≤ 2℃` 时：
1. 蜂鸣器关闭（PA8 高电平）
2. 系统状态回到 `SYSTEM_STATE_RUNNING`
3. PID 继续控制

### 4.11 模块详解 — RTC 实时时钟 (rtc.c/h)

**用途：** 为系统提供实时时间显示，每次上电从 `00:00:00` 开始计时。系统无独立 VBAT 备份电池，断电后时间复位。

**RTC 配置：**

| 参数 | 值 | 说明 |
|------|-----|------|
| 外设 | RTC | STM32F103 内置 RTC |
| 时钟源 | LSE 32.768kHz | 外部低速晶振 |
| 分频系数 | 127 + 255 | (127+1)×(255+1)=32768 → 1Hz |
| 计数方式 | 秒计数器 | 自动递增 |

**初始化流程：**
1. CubeMX 调用 `MX_RTC_Init()`，配置分频器使 RTC 以 1Hz 运行
2. USER CODE 调用 `HAL_RTC_SetTime()` 将初值设为 `00:00:00`
3. 主循环 `Display_Update()` 中每 200ms 调用 `HAL_RTC_GetTime()` 读取当前时间

**OLED 显示格式：**
```
第6行 (y=48): 14:32:05 Run:1234 s
              ↑RTC时间     ↑累计运行秒数
```

**关键 API：**

| 函数 | 说明 |
|------|------|
| `MX_RTC_Init()` | CubeMX 生成，配置 RTC 时钟和分频 |
| `HAL_RTC_SetTime(&hrtc, &sTime, RTC_FORMAT_BIN)` | 设置 RTC 初值 |
| `HAL_RTC_GetTime(&hrtc, &sTime, RTC_FORMAT_BIN)` | 读取当前时间 |

**数据结构：**
```c
typedef struct {
    uint8_t Hours;       // 时 (0-23)
    uint8_t Minutes;     // 分 (0-59)
    uint8_t Seconds;     // 秒 (0-59)
    uint8_t TimeFormat;  // 24 小时制
    uint32_t SubSeconds; // 子秒 (未使用)
} RTC_TimeTypeDef;
```

---

## 5. PID 控制算法详解

### 5.1 被控对象模型

生化培养箱的数学模型可近似为一阶惯性加纯滞后系统（FOPDT）：

$$G(s) = \frac{K \cdot e^{-\tau s}}{T_1 s + 1}$$

| 参数 | 符号 | 值 | 物理意义 |
|------|------|-----|---------|
| 系统增益 | K | 1.2 | 单位控制量引起的温度变化 |
| 惯性时间常数 | T₁ | 300s | 温度变化快慢的度量 |
| 纯滞后时间 | τ | 30s | 热量从加热器传递到传感器的时间 |

### 5.2 位置式 PID 离散化

将连续 PID 公式离散化（采样周期 Ts = 0.2s）：

$$u(k) = K_p e(k) + K_i \sum_{j=0}^{k} e(j) T_s - K_d \frac{y(k) - y(k-1)}{T_s}$$

**各项作用分析：**

| 项 | 作用 | 参数影响 |
|----|------|---------|
| P (比例) | 根据当前误差成比例地输出控制量，误差越大输出越强 | Kp↑ → 响应加快，但超调增大 |
| I (积分) | 消除稳态误差，累积历史误差 | Ki↑ → 稳态误差消除加快，但振荡风险增大 |
| D (微分) | 预测误差变化趋势，提前抑制振荡 | Kd↑ → 稳定性提高，但对噪声敏感 |

### 5.3 参数整定过程

采用 **试凑法（Ziegler-Nichols 改进）** 进行参数整定：

1. **P 整定**：Ki=0, Kd=0，逐渐增大 Kp 直到系统产生等幅振荡
2. **I 整定**：在 P 基础上加入 Ki，消除稳态误差
3. **D 整定**：在 PI 基础上加入 Kd，抑制超调

**MATLAB 仿真验证后的推荐参数：**

| 参数 | 值 | 整定依据 |
|------|-----|---------|
| Kp | 15.0 | 临界比例度的 60% |
| Ki | 0.8 | 消除稳态误差，积分分离阈值 3℃ |
| Kd | 2.5 | 抑制超调，微分对测量值求导 |

**仿真性能指标：**

| 指标 | 仿真值 | 设计目标 |
|------|--------|---------|
| 超调量 | < 15% | < 20% |
| 稳态误差 | < 0.3℃ | ±1℃ |
| 上升时间 (10%~90%) | ~200s | — |
| 调节时间 (2%) | ~600s | — |

---

## 6. MATLAB 仿真与参数整定

### 6.1 仿真模型

```
设定值 ─→(＋)──→[PID控制器]──→[限幅±100]──→[一阶惯性G1(s)]──→[纯滞后e^(-τs)]──→ 测量值
         ↑                                                      │
         └──────────────────────────────────────────────────────┘
```

### 6.2 仿真脚本

完整仿真代码见 `matlab/pid_simulation.m`，主要功能：

1. **FOPDT 模型离散化**：使用后向差分法将连续传递函数离散化
2. **延迟缓冲**：使用 FIFO 环形缓冲模拟 30s 纯滞后
3. **PID 算法模拟**：完整实现积分分离、微分对测量值、抗积分饱和
4. **性能指标计算**：超调量、上升时间、调节时间、稳态误差
5. **图形输出**：温度响应曲线、控制输出曲线、误差曲线
6. **参数扫描**（可选）：Kp/Ki/Kd 批量扫描对比

### 6.3 仿真结果分析

**典型仿真曲线：**
- 温度响应：初始 25℃ → 达到 37℃ 设定值，上升时间约 200s
- PID 输出：启动时全功率加热（100%），接近目标时 PID 线性调节，稳态时进入死区
- 误差曲线：最大误差约 5℃（启动阶段），稳态误差 < 0.3℃

---

## 7. 上位机通信协议

### 7.1 协议规范

**物理层：** USART1 TTL 电平，经 USB-to-TTL 模块转换后连接 PC  
**波特率：** 115200 bps  
**数据格式：** 8 数据位、1 停止位、无校验 (8N1)  
**应用层：** ASCII 文本，`\r\n` 换行

### 7.2 上位机发送 → 下位机

| 命令 | 格式 | 示例 | 说明 |
|------|------|------|------|
| 设置温度 | `SET:<value>\r\n` | `SET:37.5\r\n` | value 范围 5.0~50.0 |
| 停止控温 | `STOP\r\n` | `STOP\r\n` | 立即停止加热/制冷 |
| 查询状态 | `STATUS\r\n` | `STATUS\r\n` | 下位机返回当前数据 |

### 7.3 下位机 → 上位机

**主动上报（每 1 秒）：**
```
T:25.5,SET:37.0,PID:45.0,HT:60%,CL:0%,RUN:120s\r\n
```

| 字段 | 格式 | 示例 | 说明 |
|------|------|------|------|
| T | `T:xx.x` | `T:25.5` | 当前温度 |
| SET | `SET:xx.x` | `SET:37.0` | 目标温度 |
| PID | `PID:xx.x` | `PID:45.0` | PID 输出值 (0~100) |
| HT | `HT:xx%` | `HT:60%` | 加热占空比 |
| CL | `CL:xx%` | `CL:0%` | 制冷占空比 |
| RUN | `RUN:xxxxs` | `RUN:120s` | 累计运行时间 |

**命令响应：**
- `OK:37.5\r\n` — 成功
- `ERR:RANGE\r\n` — 数值超范围
- `ERR:UNKNOWN\r\n` — 未知命令

---

## 8. 调试与测试

### 8.1 硬件调试步骤

1. **电源检测**：用万用表测量 12V、5V、3.3V 电压是否正常
2. **STM32 最小系统测试**：使用 Keil + ST-Link 下载程序，确认 SWD 通信正常
3. **DS18B20 测试**：串口输出温度值，与标准温度计对比
4. **OLED 显示测试**：观察启动画面和实时数据是否正常显示
5. **按键测试**：逐键按下，确认 OLED 显示对应按键值
6. **BTS7960 输出测试**：用示波器观察 PA6/PA7 PWM 波形，确认频率 10kHz
7. **加热/制冷测试**：设定温度，用手触摸加热片和 TEC 冷面/热面，确认工作正常
8. **串口通信测试**：连接 PC，串口助手查看数据上报，发送命令验证

### 8.2 软件调试要点

| 调试项 | 方法 | 预期结果 |
|--------|------|---------|
| PWM 频率 | 示波器测 PA6 | 10kHz ±5% |
| 控制周期 | GPIO 翻转测时间 | 200ms ±1ms |
| 串口通信 | 串口助手查看 | 每秒一行状态数据 |
| 数据上报 | 串口助手接收 | 每秒一行状态数据 |
| PID 控制 | 记录温度曲线 | 超调 < 20%，稳态误差 < 1℃ |

### 8.3 常见问题及处理

| 问题 | 可能原因 | 解决方法 |
|------|---------|---------|
| DS18B20 读值异常 | 传感器未连接 / 上拉电阻缺失 | 检查 4.7kΩ 上拉，检查接线 |
| OLED 不显示 | I2C 地址错误 / 接线错误 | 确认地址 0x3C，检查 SCL/SDA |
| PWM 输出异常 | TIM3 配置错误 | 检查 Period=7199，Prescaler=0 |
| 温度控制振荡 | PID 参数不当 | 降低 Kp，适当增加 Kd |

---

## 9. 材料清单 (BOM)

| 序号 | 名称 | 型号/规格 | 数量 | 单价(元) | 备注 |
|:----:|:-----|:----------|:----:|:--------:|:-----|
| 1 | 核心板 | STM32F103C8T6 最小系统板 | 1 | ~15 | ARM Cortex-M3, 72MHz |
| 2 | 温度传感器 | DS18B20 TO-92 | 1 | ~5 | 数字温度传感器, -55~125℃ |
| 3 | OLED 显示屏 | 0.96寸 SSD1306 I2C | 1 | ~15 | 128×64 白色 |
| 4 | 开关电源 | 220V→12V/2A | 1 | ~20 | AC-DC 金属外壳 |
| 5 | 降压模块 | 7805 + AMS1117-3.3 | 各1 | ~3 | 12V→5V→3.3V |
| 6 | 电热片 | ST021 12V/10W | 1 | ~10 | 聚酰亚胺发热片 |
| 7 | 半导体制冷片 | TEC1-12706 12V/60W | 1 | ~15 | 40×40×3.8mm |
| 8 | H桥驱动模块 | BTS7960 | 1 | ~20 | 大电流电机驱动 |
| 9 | 散热风扇 | 8015 12V DC 无刷 | 1 | ~8 | 80×80×15mm |
| 10 | 散热片 | 铝制鳍片散热片 | 1 | ~5 | TEC 热端散热 |
| 11 | 按键 | 4×3 薄膜键盘 | 1 | ~5 | 矩阵键盘 |
| 12 | 有源蜂鸣器 | 5V 有源蜂鸣器 | 1 | ~2 | 报警用 |
| 13 | USB-to-TTL 模块 | CH340 / CP2102 / FT232 | 1 | ~5 | 通信模块 |
| 14 | 杜邦线 | 公-母 / 公-公 | 若干 | ~5 | 接线 |
| 15 | 面包板 | 830孔面包板 | 1 | ~8 | 原型搭建 |
| 17 | 电阻 | 4.7kΩ (1/4W) | 2 | ~1 | DS18B20/I2C 上拉 |
| 18 | PCB 板 | 5×7cm 万能板 | 1 | ~3 | 焊接成品 |
| | | | | **总计约 ¥140** | |

---

## 10. 参考文献

1. STMicroelectronics. *STM32F103x8/xB Datasheet*. Rev 15, 2015.
2. Maxim Integrated. *DS18B20 Programmable Resolution 1-Wire Digital Thermistor*. Rev 4, 2008.
3. Solomon Systech. *SSD1306 Advance Information*. Rev 1.1, 2008.
4. Infineon Technologies. *BTS7960 High Current PN Half Bridge*. Rev 2.0, 2007.
5. Åström, K.J., Hägglund, T. *PID Controllers: Theory, Design, and Tuning*. 2nd Edition, ISA, 1995.
6. 吴建清. 高精度酿酒恒温箱系统的设计与实现. 2020.
7. 郭文旭. 基于单片机的便携式医用液体恒温箱的设计与研制. 2019.
8. 杨明. 实验用恒温箱控制系统设计及其模型建立. 2018.

---

> **文档结束**
>
> 本文档配套完整源码位于 `code/temperature_control/` 目录，MATLAB 仿真脚本位于 `matlab/` 目录。
